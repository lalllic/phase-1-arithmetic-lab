// variables
const num1 = 62;
const num2 = 1;
const num3 = 29;
const num4 = 5;

// multiply; should return 62
const multiply = num1 * num2;

// generates a random number above 0
const random = Math.floor(Math.random() * 20) + 1;

// modulo; should return 4
const mod = num3 % num4;

// returns 20 as the highest number in the set
const max = Math.max(20);